---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Community app gallery for the Flipper BUSY Bar: browse and download 72x16 LED display apps, or submit your own.' } = Astro.props;
const baseUrl = import.meta.env.BASE_URL;
const canonical = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="google-site-verification" content="P2UhATGW9RNVxBMZQ-M346WAwjkiZnjTuTLwMsRRXzA" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <title>{title}</title>
    <link rel="icon" type="image/png" href={`${baseUrl}favicon.png`} />
  </head>
  <body>
    <header class="site-header">
      <div class="container">
        <a href={baseUrl} class="logo">
          <img src={`${baseUrl}brand/logo.svg`} alt="BUSY" class="logo-img" />
          <span class="logo-text">Apps</span>
        </a>
        <nav class="nav">
          <a href={baseUrl} class="nav-link">Apps</a>
          <a href={`${baseUrl}authors/`} class="nav-link">Authors</a>
          <a href="https://github.com/maxswinkels/busybar-apps/blob/main/CONTRIBUTING.md" target="_blank" class="nav-link">Submit an app</a>
          <a href="https://github.com/maxswinkels/busybar-manager" target="_blank" class="nav-link">Manager</a>
          <a href="https://github.com/maxswinkels/busybar-emulator" target="_blank" class="nav-link">Emulator</a>
        </nav>
      </div>
    </header>

    <main class="site-main">
      <slot />
    </main>

    <footer class="site-footer">
      <div class="container">
        <p class="footer-text">
          © {new Date().getFullYear()} <a href="https://github.com/maxswinkels" target="_blank" rel="noopener">Max Swinkels</a> · <a href="https://github.com/maxswinkels/busybar-apps/blob/main/LICENSE" target="_blank" rel="noopener">MIT license</a>
        </p>
        <p class="footer-text">
          Unofficial project. "BUSY Bar" is a trademark of <a href="https://busy.app" target="_blank" rel="noopener">Flipper Devices</a>. Bundled fonts &amp; artwork © Flipper Devices (CC-BY 4.0 / SIL OFL 1.1).
        </p>
      </div>
    </footer>
  </body>
</html>

<style>
  :global(html, body) {
    margin: 0;
    padding: 0;
    font-family: var(--font-ui);
    font-size: 15px;
    line-height: 1.6;
    color: var(--color-text);
    background-color: var(--color-bg);
  }

  :global(a) {
    color: var(--color-primary);
    text-decoration: none;
  }

  :global(a:hover) {
    text-decoration: underline;
  }

  :global(h1, h2, h3) {
    margin-top: 0;
    font-weight: 600;
    line-height: 1.2;
  }

  :global(h1) {
    font-size: 28px;
  }

  :global(h2) {
    font-size: 20px;
    margin-top: 24px;
  }

  .site-header {
    background: white;
    border-bottom: 1px solid var(--color-border);
    padding: 16px 0;
    position: sticky;
    top: 0;
    z-index: 100;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .site-header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    display: flex;
    gap: 8px;
    align-items: center;
    font-size: 18px;
    font-weight: 600;
    letter-spacing: -0.02em;
    color: inherit;
    text-decoration: none;
  }

  .logo:hover {
    text-decoration: none;
  }

  .logo-img {
    height: 28px;
    display: block;
  }

  .logo-text {
    color: var(--color-text);
  }

  .nav {
    display: flex;
    gap: 24px;
  }

  .nav-link {
    font-size: 14px;
    font-weight: 500;
    color: var(--color-text-secondary);
    transition: color 0.2s;
  }

  .nav-link:hover {
    color: var(--color-primary);
    text-decoration: none;
  }

  .site-main {
    flex: 1;
  }

  .site-footer {
    margin-top: 48px;
    padding: 40px 0;
    border-top: 1px solid var(--color-border);
    background: #fafafa;
    color: var(--color-text-secondary);
    font-size: 13px;
  }

  .site-footer .container {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .footer-text {
    margin: 0;
  }

  .footer-text a {
    color: var(--color-text-secondary);
    text-decoration: underline;
  }

  .footer-text a:hover {
    color: var(--color-primary);
  }

  @media (max-width: 768px) {
    /* Four nav items do not fit next to the logo on a phone; tighten, then wrap. */
    .site-header .container {
      flex-wrap: wrap;
      gap: 4px var(--sp-md);
    }

    .nav {
      gap: var(--sp-md);
    }

    .nav-link {
      font-size: 13px;
    }
  }
</style>
